import PageTransition from '../components/layout/PageTransition';

const Work = () => (
  <PageTransition>
    <section className="sr-only" aria-label="Work projects">
      <h1>Work</h1>
    </section>
  </PageTransition>
);

export default Work;
